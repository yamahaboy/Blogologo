export type SignUpProps = {
  email: string;
  password: string;
  name: string;
  surname: string;
};

export type SignInProps = {
  email: string;
  password: string;
};

export type UserProps = {
  id: string;
  name: string;
  surname: string;
  email: string;
  password: string;
};

export type LabelProps = {
  name: string;
  surname: string;
};
